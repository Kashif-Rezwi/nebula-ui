{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "rm -rf node_modules .next && npm ci --legacy-peer-deps",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}